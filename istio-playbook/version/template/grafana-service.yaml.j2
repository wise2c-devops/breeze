apiVersion: v1
kind: Service
metadata:
  name: grafana
spec:
  type: NodePort
  ports:
  - name: http
    nodePort: {{ grafana_nodeport }}
    port: 3000
    targetPort: http
  selector:
    app: grafana
